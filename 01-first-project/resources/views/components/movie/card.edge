<div class="{{html.classNames(['w-full max-w-sm bg-white border border-slate-200 rounded-lg shadow-md', ($props.class || '')])}}" {{ $props.except(['movie', 'class' ]).toAttrs() }}>
  <div class="relative">
    <img src="{{ movie.posterUrl }}" class="w-full h-auto" style="max-height: 100px;" />
  </div>
  <div class="p-7">
    <h2 class="mb-2 text-lg font-bold leading-none tracking-tight">
      {{ movie.title }}
    </h2>
    <p class="mb-5 text-slate-500">
      {{ movie.summary }}
    </p>
    @button({ href: route('movies.show', { slug: movie.slug }) })
    {{ movie.title}}
    @end
  </div>
</div>